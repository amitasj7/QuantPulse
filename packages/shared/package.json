{
  "name": "@quantpulse/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types, constants, and utilities for QuantPulse",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0"
  }
}
